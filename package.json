{
  "name": "gaga-dog-summary",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check index.js && node --check memory-core.js && node --check prompts.js",
    "test": "node --test tests/*.test.mjs"
  }
}
